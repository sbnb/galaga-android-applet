#
# specify files and directories that should be ignored by git and not tracked

# ignore everything under ~project/bin (class files rebuilt by Eclipse on builds)
bin/
gen/
.pmd

# created by android tool, not to be checked into src control
local.properties

